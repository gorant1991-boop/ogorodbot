2026-04-03

- Прод `https://ogorod-ai.ru` сейчас отдаёт сборку из `HEAD`, а не из текущего dirty worktree. По HTML видно старый `index.html` без новых PWA-мета/manifest и со старыми asset-хешами.
- `HEAD:src/supabase.ts` всё ещё ходит напрямую в таблицы Supabase через anon-клиент (`garden_data`, `notifications`, `diary`, `seasons`, `analytics_events`, `billing_payments`).
- В рабочем дереве фронт уже переведён на edge function `user-data` с проверкой identity (email/VK/Telegram/review), а рядом лежат RLS-миграции, включая `202604010006_lock_down_anon_table_access.sql`, которая снимает anon-политики с пользовательских таблиц.
- Вероятная причина жалобы "пропала авторизация": облако/деплой выкатило старый фронт, а backend/RLS уже смотрит на новую схему доступа. Старый фронт больше не может читать/писать пользовательские данные напрямую и выглядит как сломанный auth.
- Для страховки добавлены локальные guard-ы: `scripts/check-build-env.mjs` + `prebuild` в `package.json`, а в `src/supabase.ts` добавлен fail-fast, если отсутствуют `VITE_SUPABASE_URL` или `VITE_SUPABASE_ANON_KEY`.
- Подкручен тон AI-советов: в prompts для фронта и backend-уведомлений заметки пользователя об участке теперь явно считаются наблюдениями, а не проверенными фактами. Модель просим формулировать осторожно и не повторять заметки дословно без пользы.
- В weekly plan `siteNotes` больше не вставляются как "факты"; вместо этого используются мягкие формулировки вида "если ваши заметки по участку всё ещё актуальны".
- В owner analytics backend добавлен допуск по Telegram-личности: доступ даётся не только по owner VK/email, но и по Telegram, если он совпадает с привязанным owner-аккаунтом или со значениями в `OWNER_TELEGRAM_IDS` / `OWNER_TELEGRAM_USERNAMES`.
- После этих правок заново задеплоены `supabase/functions/user-data` и фронт на `ogorod-ai.ru`.
- На экране авторизации предупреждение про создание аккаунта и привязку способа входа вынесено в отдельный заметный жёлтый блок; раньше низ блока был стилизован светлым текстом и терялся на фоне.
- По погоде локально проверен `VITE_OPENWEATHER_API_KEY`: OpenWeather отвечает `cod=200`, ключ рабочий. Добавлена fail-fast проверка ключа в `scripts/check-build-env.mjs`, чтобы релиз больше не уходил без погодного ключа.
- `useWeather` и `useForecast` теперь возвращают понятные причины сбоев: нет города, нет ключа, ключ недействителен, город не найден, сервис временно недоступен. Эти тексты показаны в UI вместо общего "Погода недоступна".
- Во втором шаге онбординга текст явно говорит, что можно сразу добавить несколько объектов.
- После этих правок фронт заново опубликован; прод начал отдавать `index-DeZPQAjo.js` и `index-69DR8hrF.css`.
- Для встроенных браузеров из Telegram, VK, Instagram, Facebook/Messenger и generic webview добавлен детект в `src/utils/browser.ts`.
- На auth-экране и на главной теперь показывается явный баннер "лучше открыть в обычном браузере" с инструкцией под конкретный контейнер и кнопкой копирования ссылки.
- В embedded webview отключена регистрация service worker в `src/main.tsx`, чтобы не плодить дополнительные странности в контейнерах, где и так ломаются логины/возвраты/часть системных функций.
- После этих правок фронт заново опубликован; прод начал отдавать `index-BxTOwPjB.js` и `index-DwaQX0fb.css`.
- Weekly plan перестроен так, чтобы не повторять один и тот же влажностный дисклеймер почти в каждой записи: теперь учитываются сигналы `siteNotes`, тип почвы, проблемы с дренажом, ветер, тень и тип объекта, а для fallback-задач действие выбирается не только как "полив", но и из других доступных операций.
- Для переувлажнённых участков и высоких грунтовых вод формулировки смягчены: вместо жёсткого "влага держится дольше" советы предлагают сначала проверять фактическую влажность у корней и отдельно проговаривают, что это не обязательно вредно само по себе.
- Практический и научный факт на главной теперь можно раскрывать и сворачивать без лишнего перезапроса; после перезагрузки экрана карточки не остаются визуально раскрытыми автоматически.
- В чате вопрос "по культуре" больше не показывает сырой AI-подобный промт: для пользователя выводится короткий человекочитаемый запрос с ключевыми характеристиками, а полный контекст остаётся скрытым и уходит только модели.
- В профиле из набора инструментов убраны `лейка` и `шланг`, а при сборке агрономического контекста они фильтруются как базовые вещи по умолчанию; советы теперь сильнее опираются на реальные удобрения и инструменты пользователя и не должны так свободно их выдумывать.
- После этих правок фронт снова опубликован, `generate-morning-advice` задеплоен повторно, а cache-busting запрос к продовому `index.html` уже показывает новые asset-файлы `index-BfGxm3rX.js` и `index-DohDetLt.css`.
- Добавлен лёгкий слой помощи вместо россыпи баннеров: у новых пользователей после онбординга показывается одноразовое вступительное знакомство, а дальше на основных вкладках (`Главная`, `Растения`, `Луна`, `Дневник`, `Профиль`) появляются короткие контекстные подсказки с возможностью скрыть отдельную подсказку или сразу выключить всё.
- Режим подсказок теперь хранится прямо в `onboarding` (`helpHintsEnabled`, `introSeen`, `seenHints`), а не в локальном временном состоянии, поэтому настройка переживает обычные обновления и повторные входы.
- В профиле появился отдельный блок `Подсказки`: можно выключить контекстные подсказки полностью или заново открыть вступительное ознакомление.
- После публикации фронта был короткий лаг CDN, но повторный cache-busting запрос уже показал новый продовый билд `index-c6SgQ5Nm.js` и `index-DPCXXj8j.css`.
- Блок `Поделиться в VK` в профиле упрощён до одной кнопки: теперь вместо двух вариантов поста всегда собирается одна информативная карточка про огород и пользу приложения, а логика бонуса `+1 день Базовой` за репост сохранена.
- Share-card для VK переписана под более понятный оффер: акцент на поливе, подкормках, погодных рисках и бесплатном первом совете, без расплывчатого маркетингового текста.
- Weekly plan теперь лучше связывает задачу `подкормка` с реальными удобрениями пользователя: если удобрения заведены в профиле, в причине задачи подсказывается, на что из имеющегося смотреть в первую очередь; если нет, совет честно говорит подбирать подкормку по стадии, а не "что попало".
- Для весенних ягодных многолетников (`клубника`, `малина`, `смородина` и т.п.) в fallback-плане поднят приоритет сезонных операций вроде осмотра после зимы, санитарной обрезки, подвязки и стартовой подкормки.
- Повторяющийся хвост про сырость и влагу в weekly plan дополнительно заужен: теперь такие напоминания массово не лепятся ко всем `Осмотрам`, а появляются только там, где есть локальный риск по объекту или почве.
- После этих правок фронт снова собран и опубликован; cache-busting проверка показала новые продовые ассеты `index-DmyIj6pT.js` и `index-DPCXXj8j.css`.
- Для культур добавлена структурная память операций `operationMemory`: теперь приложение хранит последние поливы, подкормки, подвязки, прищипки, опоры, обрезку и опрыскивания по каждой культуре, а не только надеется на свободный текст в дневнике.
- Память операций подтягивается и из новых записей дневника, и из уже существующей истории: при загрузке аккаунта и при синхронизации дневника старые записи пересобирают `operationMemory`, чтобы логика не начиналась "только с сегодняшнего дня".
- Контекст AI-чата и утренних советов теперь видит краткую сводку последних операций по каждой культуре; это должно уменьшить повторы вроде "подкормите" сразу после уже отмеченной подкормки.
- В weekly plan добавлена защита от повторов по недавним операциям: fallback-план старается не ставить недавно отмеченную подкормку, подвязку, прищипку и похожие работы слишком быстро повторно.
- В `getOps()` добавлено структурное `опрыскивание`, чтобы его можно было отмечать в дневнике как отдельную операцию, а не только писать свободным текстом.
- Weekly plan теперь подтягивает прогноз и учитывает дождь: в дождливый день fallback старается не выбирать `полив`, а для `полива` и `опрыскивания` в нормализованном плане добавляется явная погодная поправка "проверьте влажность" / "лучше перенести на сухое окно".
- После этих правок фронт снова собран, опубликован и на проде подтверждён новый билд `index-jhlVlj4j.js`, `index-DPCXXj8j.css`, `index-legacy-CBNMDwg7.js`; `generate-morning-advice` заново задеплоен в Supabase.
- Дневник получил семантику записей без миграции БД: в текст записи теперь встраиваются мета-строки `Сорт`, `Тип записи` (`done` / `observation` / `plan`) и `Детали`. Это позволяет отличать "сделал" от "наблюдаю" и "планирую", не меняя таблицу `diary`.
- Для операций добавлены короткие уточнения в UI: например для полива можно отметить "слегка пролил" или "хорошо промочил", для подкормки — "под корень" / "по листу", для опрыскивания — "профилактика" / "от болезней" / "от вредителей". Эти детали попадают в `operationMemory` и в AI-контекст.
- Быстрые отметки "сделано сегодня" теперь считаются выполнением только если запись действительно имеет смысл `done`; наблюдения и планы больше не должны ложно закрывать задачу дня и портить недельную статистику.
- В `OnboardingData` добавлены `rainObservations`: пользователь может коротко уточнить фактический дождь по участку (`soaked`, `light`, `missed`), а фронт и AI используют это как более надёжный локальный сигнал, чем общий прогноз по городу.
- На экране `Луна`/weekly plan появился компактный блок "Уточните дождь по факту" для сегодняшнего дня, если прогноз обещал дождь или в плане есть чувствительные задачи вроде полива/опрыскивания.
- Локальная сборка после этих правок дала новые ассеты `index-DLcEcche.js`, `index-CNGHaHfQ.css`, `index-legacy-BBYJETdp.js`; деплой опубликован, но GitHub Pages ещё несколько минут может отдавать старый `index.html` по CDN-кэшу.
- Добавлено склонение названий культур в заметных пользовательских фразах: fallback weekly plan больше не пишет `за томат` и `у огурец`, а AI-вопрос по культуре формируется как `по базилику` / `по смородине`. После публикации продовый `index.html` начал отдавать `index-DLcEcche.js`, `index-CNGHaHfQ.css`, `index-legacy-BBYJETdp.js`.
- Починен рассинхрон "тариф работает, а покупка не видна": в `ProfileScreen` добавлен честный fallback для активного `base/pro` без `onboarding.subscription`, а в `user-data` `saveUserRow()` теперь сохраняет текущий серверный `plan`, если у legacy-профиля нет структурной подписки, чтобы обычное сохранение профиля не сбрасывало такой доступ до `free`.
- `user-data` задеплоен в проект `ghlvnpsuklruauuabdev`. Фронт опубликован, но GitHub Pages ещё отдаёт предыдущий `index.html` с ассетами `index-DFQ29CEV.js`, `index-CNGHaHfQ.css`, `index-legacy-YhAs9-CJ.js`; новая карточка подписки должна появиться после обновления CDN-кэша.
- Для owner-аналитики удалось снять живые цифры напрямую из Supabase через `service_role`, без UI: всего пользователей `11`, активных профилей `11`, платных активных `2`, оплат всего `2`, выручка `450 ₽`, записей в дневнике `30`, онбордингов `9`, открытий оплаты `12`, VK-репостов `20`, рефералов `3`.
- Пользователь уточнил, что текущие `2 оплаты` и `20 репостов VK` фактически его собственные/тестовые. Значит текущая продуктовая аналитика почти целиком загрязнена owner/test-активностью и пока не подтверждает внешний спрос.
- По рекламе VK на вечер `3 апреля 2026` пользователь показал скрин с метриками: `1955` показов, `2` клика, расход `86,04 ₽`, `CTR 0,1%`, `eCPC 43,02 ₽`, стартов просмотра `811`, досмотров `100%` — `149`. Вывод: реклама технически крутится, но холодной аудитории пока не залетает.
- Зафиксирован рабочий вывод для следующих сессий: не считать текущие оплаты/репосты как PMF-сигнал, а в ближайших итерациях либо отделять owner/test события в аналитике, либо смотреть только на внешние метрики `не ты -> онбординг -> открытие оплаты -> оплата`.
