2026-04-04

- По рекламным креативам для VK/лендинга зафиксирована отдельная итерация: всего в папке `ads/` на текущий момент `45` файлов.
- Разбивка по типам файлов в `ads/`: `7` HTML-макетов, `25` PNG-файлов, `13` JPG-файлов.
- По стадиям работы над креативами: `7` исходных HTML-макетов, `5` raw-рендеров из headless Chrome, `21` промежуточный/отладочный файл, `12` финальных экспортов (это `6` финальных концептов в парах `png/jpg`).
- Важно для следующих сессий: из-за бага headless Chrome на macOS рендеры регулярно получали белый хвост снизу; для чистого финала надёжный пайплайн такой: `Chrome raw -> определить first_white_row через Pillow -> crop -> собрать чистый canvas через Pillow`.
- Финальные концепты, которые реально можно показывать/использовать:
  - `today-ogorod-real-square-ai-final` — квадратный креатив в стиле реального экрана приложения, с упоминанием `AI-помощника`.
  - `today-ogorod-real-vertical-final` — честкий вертикальный экран приложения без телефона и без выдуманного UI.
  - `today-ogorod-beautiful-portrait-final` — более арт-дирекшн версия `1080x1350` с реальным экраном приложения как центральным визуалом и акцентом на `AI-помощнике`.
- Пользователь явно попросил уйти от выдуманного интерфейса и телефонных мокапов: для рекламных материалов приоритет теперь за реальными экранами приложения, а не за фантазийными экранами.
- По рекламе VK из предыдущего дня уже есть зафиксированные живые цифры для отчёта: `1955` показов, `2` клика, расход `86,04 ₽`, `CTR 0,1%`, `eCPC 43,02 ₽`, стартов просмотра `811`, досмотров `100%` — `149`. Вывод остаётся тем же: реклама технически крутится, но холодной аудитории пока слабо объясняет ценность продукта.
- По аналитике сайта вскрылось важное ограничение: отдельный `page_view` / `session_start` сейчас не трекается, поэтому “посещения сайта” как самостоятельную метрику показать честно нельзя. Ближайшие прокси в owner analytics — `auth_success`, `new_users`, `active_profiles`, `onboarding_complete`, `checkout_opened`.
- Попытка вытащить live owner analytics с этой машины частично удалась технически, но не довелась до финала: в Chrome нашли локальные следы `ogorod-ai.ru`, старую owner Supabase-сессию и Telegram auth, однако owner email session уже протухла (`refresh_token_not_found`), а текущий Telegram payload из LevelDB не удалось воспроизвести byte-to-byte для server-side проверки подписи. Значит без ручного owner-входа нельзя утверждать live-изменение посещаемости.
- В кодовую базу добавлен базовый слой веб-аналитики для `Яндекс Метрики`: инициализация счётчика в `src/main.tsx`, helper `src/utils/webAnalytics.ts`, виртуальные SPA-hit'ы по смене экранов (`/onboarding`, `/app/main`, `/app/plants`, `/chat`, `/owner-analytics`) и проброс ключевых целей из уже существующих продуктовых событий.
- Счётчик Метрики шлёт цели `view_main`, `auth_success`, `onboarding_complete`, `checkout_opened`, `payment_succeeded`; часть шумных целей защищена дедупликацией через `sessionStorage`, чтобы не плодить повторы при перезагрузках в одной сессии.
- Проверка после внедрения прошла: `npm run build` и `npm run lint` зелёные.
- Важный операционный нюанс: в локальном `.env` переменная `VITE_YANDEX_METRICA_ID` пока отсутствует, поэтому до добавления реального ID и деплоя код останется неактивным.
- Фронт с новым слоем аналитики уже опубликован на `ogorod-ai.ru` через `gh-pages`.
- На момент проверки прод отдавал новые ассеты `assets/index-Bgi9u0Dm.js`, `assets/index-CNGHaHfQ.css`, `assets/index-legacy-DOtcpE7R.js`, `assets/polyfills-DV5l0lZE.js`, `assets/polyfills-legacy-R89NMl4U.js`.
- Но так как `VITE_YANDEX_METRICA_ID` в среде сборки всё ещё пустой, код Метрики на прод ушёл в "спящем" режиме: логика встроена, но без реального counter ID никаких hit/goal наружу не отправляется.
- Позже в тот же день прод был перевыкатан уже с реальным счётчиком Метрики `108388834`; текущие цели: `view_main`, `auth_success`, `onboarding_complete`, `checkout_opened`, `payment_succeeded`.
- Починен переход на скрытый owner dashboard: `?view=owner-analytics` теперь всегда открывает сам экран, а не тихо падает в обычный сайт, если локальный `isOwner` не распознался на фронте.
- Собран и задеплоен MVP фотодиагностики без хранения:
  - UI живёт во вкладке болезней и позволяет выбрать культуру, загрузить 1 фото, добавить заметку и отправить на разовый AI-разбор.
  - Клиент сжимает фото до JPEG через canvas, чтобы не гонять большие исходники.
  - Backend реализован внутри `user-data` как action `analyze_plant_photo` и рассчитан на OpenAI Responses API с vision + structured JSON answer.
  - Локальные аналитические события добавлены как `photo_diagnosis_requested` и `photo_diagnosis_completed`.
- Внешний блокер для живой фотодиагностики: в продовом Supabase на текущий момент отсутствует секрет `OPENAI_API_KEY`. Поэтому UI и endpoint уже на проде, но реальный AI-разбор начнёт работать только после добавления этого секрета. Сейчас вместо этого пользователь увидит понятное сообщение, что фотодиагностика ещё не настроена на сервере.
- Поверх фото-MVP добавлена бесплатная кнопочная диагностика по симптомам для всех культур. Она теперь главный блок на вкладке болезней: пользователь выбирает культуру, часть растения, главный симптом, зону поражения и условия, а движок возвращает до 3 наиболее правдоподобных версий с проверками и безопасными действиями.
- В движке симптомов заведены группы культур (`solanaceae`, `cucurbits`, `brassicas`, `alliums`, `roots`, `leafy`, `berries`, `herbs`, `legumes`, `corn`) и набор правил для грибковых пятнистостей, налёта, вредителей, дефицитов, перелива, пересушки, плодовых и прикорневых гнилей, холодового стресса и ожога после подкормки.
- После внедрения кнопочной диагностики локальные проверки зелёные: `npm run build` и `npm run lint` прошли. Фронт опубликован ещё раз; локальный свежий билд содержит `dist/assets/index-BaKMsrMT.js`, но `curl` по `ogorod-ai.ru` сразу после выката ещё отдавал предыдущий asset-хэш (`index-CCudyVwg.js`) из-за CDN-кэша GitHub Pages.

- Временнo убрана фотодиагностика из UI вкладки болезней: на проде осталась только бесплатная кнопочная диагностика по симптомам.
- Под табами экрана растений добавлена заметная зелёная подсказка про новую бесплатную диагностику по симптомам с кнопкой перехода во вкладку `Болезни`.
- После удаления фото-блока локальные проверки снова зелёные: `npm run lint`, `npm run build`; фронт перевыкатан с `VITE_YANDEX_METRICA_ID=108388834`.
- На момент после выката прод должен начать отдавать новый бандл `index-Ja01huHs.js`, но GitHub Pages может ещё несколько минут держать старый HTML в CDN-кэше.
- Кнопочная диагностика по симптомам теперь учитывает список удобрений пользователя прямо в UI: если удобрения заполнены, экран показывает, что помнит их, а для результатов по питанию/ожогу после подкормки добавляет отдельный блок со сверкой имеющихся средств и предупреждением не кормить вслепую.
- После этой правки фронт снова прошёл `npm run lint` и `npm run build`, затем был перевыкатан на прод с `VITE_YANDEX_METRICA_ID=108388834`.
- Для Яндекс Директа собран новый креатив под бесплатную диагностику в формате `4:3` (основной безопасный формат для одного изображения по докам Директа): исходник `ads/yandex-direct-diagnosis-free.html`, финалы `ads/yandex-direct-diagnosis-free-final.png` и `ads/yandex-direct-diagnosis-free-final.jpg`, размер `1200x900`, без белого хвоста снизу.
- По просьбе пользователя для ускорения рекламных креативов установлен локальный редактор `Inkscape` через Homebrew Cask. Приложение фактически лежит в `/Applications/Inkscape.app`; бинарь доступен как `/Applications/Inkscape.app/Contents/MacOS/inkscape` и подходит и для ручной правки, и для точного CLI-экспорта баннеров.
- Онбординг радикально упрощён: из стартового сценария убраны шаги выбора объектов, культур и статусов посадки. Теперь он спрашивает только город + тип местности, а затем имя (опционально), уровень советов и удобное время уведомлений.
- В текстах онбординга и первого приветствия явно сказано, что культуры и места выращивания добавляются уже после входа во вкладке `Растения`.
- После упрощения онбординга фронт снова прошёл `npm run lint` и `npm run build`, затем был перевыкатан на прод с `VITE_YANDEX_METRICA_ID=108388834`.
- Для Яндекс Директа собран ещё один креатив с вертикальным смартфоном и правым блоком фишек: `ads/yandex-direct-phone-features.html`, финалы `ads/yandex-direct-phone-features-final.png` и `ads/yandex-direct-phone-features-final.jpg`, размер `1200x900`.
- Главная вкладка `main` была признана перегруженной для холодного трафика: одновременно жили `Сегодня в огороде`, вечерний фокус, недельная сводка, план недели, совет дня, факты от AI, апселл и чат. Решение на 4 апреля — жёстко сузить главный сценарий до вопроса `что сделать сегодня`.
- В `src/App.tsx` из главной убраны `Вечерний фокус`, карточка `Пульс недели` и оба блока/кнопки с интересными фактами от AI. Вместо этого добавлено понятное пустое состояние для нового пользователя: одна карточка с CTA `Добавить культуры`, без апселла недельного плана до тех пор, пока в огороде нет посаженных культур.
- Плейсхолдер советов на главной теперь зависит от состояния огорода: если культур ещё нет, экран не обещает абстрактный “первый совет”, а прямо объясняет, что сначала нужно добавить культуры во вкладке `Растения`.
- После упрощения главной прошли `npm run lint` и `npm run build`; затем фронт снова задеплоен на `ogorod-ai.ru` с `VITE_YANDEX_METRICA_ID=108388834`.
- На момент проверки после деплоя прод уже отдавал новые ассеты главного экрана: `/assets/index-CWVjYG0y.js`, `/assets/index-WVXNflEi.css`, `/assets/index-legacy-Cbtwf7NG.js`.
- По свежему аудиту слабых мест закрыт высокий риск рассинхронизации identity-хеша: `hashIdentityToUserId` вынесен в единый файл `shared/identity.ts` и теперь используется фронтом (`App.tsx`, `src/supabase.ts`, `src/utils/telegram.ts`) и edge-функцией `supabase/functions/user-data/index.ts`.
- Закрыта утечка `VITE_WORKER_API_KEY` в клиент: прямые вызовы `garden-agent.gorant1991.workers.dev` убраны из фронта. Теперь чат с агрономом и AI-недельный план идут через защищённые action'ы `ask_agronomist` и `load_weekly_plan` внутри `user-data`, где `WORKER_API_KEY` читается только на сервере.
- После правки клиентский билд проверен grep'ом по `dist`: ни `VITE_WORKER_API_KEY`, ни `garden-agent.gorant1991.workers.dev` в статических ассетах больше не осталось.
- Удалён лишний мусор в репозитории: пустые директории `supabase/functions/save-push-subscription` и `supabase/functions/delete-push-subscription` убраны из `supabase/functions` и отправлены в `~/.Trash`, а не удалены безвозвратно.
- Устранена путаница типов прогноза: фронтовый погодный тип в `useForecast` переименован в `WeatherForecastDay`, а старые неиспользуемые дубликаты `ForecastDay` / `WeekTask` / `WeekDay` из `src/utils/types.ts` удалены.
- Локальные проверки после этого прохода зелёные: `npm run lint` и `npm run build` прошли. Проверку edge-функции через `deno check` сделать не удалось, потому что `deno` в этом окружении отсутствует.
- Эти правки задеплоены: `supabase functions deploy user-data` успешно обновил edge-функцию в проекте `ghlvnpsuklruauuabdev`, причём в деплой ушёл и `shared/identity.ts`.
- Фронт после этого тоже опубликован на `ogorod-ai.ru` с новым локальным бандлом `dist/assets/index-DW2ZINzC.js` и `dist/assets/index-legacy-BcjpEyyX.js`.
- Но сразу после публикации `curl https://ogorod-ai.ru` всё ещё показывал старый HTML из CDN GitHub Pages с ассетами `/assets/index-CWVjYG0y.js`, `/assets/index-WVXNflEi.css`, `/assets/index-legacy-Cbtwf7NG.js`; значит, как и раньше, нужно закладывать несколько минут на обновление кэша.
- Создан долгоживущий `MEMORY.md` по `ogorodbot`: это теперь основной опорный файл для следующих сессий и будущих правок, а не только сырые дневные заметки.
