2026-04-02

- Добавлен Telegram login как новый auth-провайдер рядом с email и VK.
- Новый файл `src/utils/telegram.ts`: localStorage state, нормализация payload, derived userId через hash от `telegram:<id>`.
- `user-data` теперь умеет server-side проверять Telegram login widget payload по `TELEGRAM_BOT_TOKEN` через HMAC SHA-256.
- В `App.tsx` добавлен Telegram widget на auth-экране и поддержка сохранённой Telegram-сессии.
- В `ProfileScreen.tsx` блок аккаунта теперь показывает вход через Telegram.
- Обновлён `.env.example`: нужен `VITE_TELEGRAM_BOT_USERNAME`.
- Задеплоены `user-data` и фронт на `ogorod-ai.ru`.
- Telegram ещё не активирован в окружении: сейчас не заданы `VITE_TELEGRAM_BOT_USERNAME` и `TELEGRAM_BOT_TOKEN`.
- Сезонные подписки убраны из витрины и из YooKassa: оставлены только месячные тарифы и разовый план на 7 дней. Поддержка старых `seasonal` в данных сохранена для совместимости.
- По скриншоту из кабинета YooKassa: договор уже проверен и одобрен ("Проверили договор — всё отлично"). Следующий шаг не верификация, а настройка приёма платежей/связка сайта с платёжной формой; как временный обходной вариант в кабинете уже доступно выставление счетов.
- Проверен боевой Supabase-проект `ghlvnpsuklruauuabdev`: функции `create-yookassa-payment`, `get-yookassa-payment-status` и `yookassa-webhook` задеплоены и обновлены.
- В `supabase/functions/_shared/yookassa.ts` исправлена логика применения оплаты: месячная подписка теперь считается от момента успешной оплаты и корректно продлевает текущий активный срок, а не перетирает его датой создания платежа.
- В продовом Supabase выставлены `YOOKASSA_SHOP_ID=1313824` и `YOOKASSA_ENABLE_RECURRING=false`.
- В продовом Supabase выставлен и `YOOKASSA_SECRET_KEY`; боевые secrets ЮKassa теперь на месте. Значения в памяти не сохранять.
- Главный экран разгружен: погода и weekly plan теперь грузятся лениво только там, где реально нужны, а не глобально при каждом открытии приложения.
- Добавлена пользовательская офлайн-копия: в профиле появилась кнопка обновления, локально сохраняются данные огорода, недавний дневник, последний совет, погода и план недели; при открытии без сети приложение умеет подняться из этого слепка.
- Дневник получил офлайн-показ сохранённых записей с понятным предупреждением, что создание и удаление вернутся после появления интернета.
- Юридическая страница `terms-privacy.html` приведена ближе к реальному продукту: убраны сезонные тарифы, добавлен Telegram-вход и разовый `План на 7 дней`.
- Owner-аналитика вынесена в отдельный приватный экран по ссылке `/?view=owner-analytics`; backend по-прежнему отдаёт данные только владельцу.
- В owner-аналитику добавлены расширенные 7/30-дневные метрики: новые пользователи, активные профили, успешные оплаты, выручка, события воронки и маркетинговые действия.
- AI-советы и серверные уведомления усилены правилом "только по-русски"; добавлен послепроцессинг, чтобы убирать утечки вроде `culture/crop/watering`.
- В профиле появилась кнопка тестовой отправки в Telegram; деплой обновлён для `user-data`, `generate-morning-advice` и фронта.
- Weekly plan на фронте теперь отправляет в worker больше контекста и имеет локальный fallback, если внешний ответ пустой или отваливается.
- Объекты по всему UI начали называться по-человечески: `Грядка`, `Теплица`, `Парник`, с явными подсказками вроде `грядка у теплицы`.
